# Aborted run — not acceptance evidence

The node set changed from eight to seven during the baseline. The run was
aborted before any fault injection, and its two surviving local k6 processes
were terminated. This directory is retained only as proof that the entry gate
correctly stopped on cluster churn.
