# Opt-in overlay for the self-heal live executor.
# Only enable in a dev/demo namespace approved for the executor allowlist.
# This overlay deploys the executor with live Kubernetes mutation still disabled.
aiopsLiveExecutor:
  enabled: true
  image:
    digest: sha256:8b7e278cf9d715d28b5159f1521b13b1783a911ba33a1d9fc1572f98d7da1f20
  auth:
    existingSecret: aiops-live-executor-token
    tokenKey: token
