# Managed by tf2-corp-platform secure delivery pipeline.
# Change trail: @hungxqt - 2026-07-20 - Selective service digest promote into service-digest/.
components:
  frontend:
    imageOverride:
      # The promoted multi-arch index was expired by the production ECR
      # lifecycle policy. Pin the surviving linux/arm64 manifest used by the
      # production node architecture until the next signed index promotion.
      digest: "sha256:dc8d9e42556b5184b7e6a9a471bd836da5e21c42fd85ad2d90594b4f5729b573"
